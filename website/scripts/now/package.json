{
  "scripts": {
    "build": "echo \"dummy build\"",
    "build:app": "cd ../.. && npm run build",
    "deploy:prod": "node build && npx vercel --prod",
    "deploy": "node build && npx vercel"
  },
  "dependencies": {
    "@sveltech/ssr": "^0.0.9",
    "fs-extra": "^8.1.0"
  }
}
